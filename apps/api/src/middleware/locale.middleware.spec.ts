import { LocaleMiddleware } from './locale.middleware';
import { LocaleService } from '@services/locale/locale.service';

describe('LocaleMiddleware', () => {
  // it('should be defined', () => {
  //   expect(new LocaleMiddleware(new LocaleService()))
  //     .toBeDefined();
  // });
});
